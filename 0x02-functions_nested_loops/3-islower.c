#include "main.h"
/**
 * _islower - function to check for lowercase character
 * @c: char
 * Return: 1 if char is lowercase, else 0
 */
int _islower(int c)
{
	int c;

	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);
	{
	_putchar('\n');
	}
}
