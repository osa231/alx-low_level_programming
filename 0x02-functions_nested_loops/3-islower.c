#include "main.h"
/**
 * _islower - function to check c for lowercase character
 * Return: 1 if char is lowercase, else 0
 */
void _islower(void)
{
	int c;

	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);
	{
		_putchar('\n');
}
